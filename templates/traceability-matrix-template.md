# Traceability Matrix — `<project>` — `<YYYY-MM-DD>`

> Produced by the Traceability agent. Every column derives from artifact content — do not
> edit manually. Re-run via `/iconix-status` to refresh.
>
> **BR-NNN column:** populated when `docs/business-rules.md` exists
> and Phase 5d Step 4 has annotated UC files. Omit the column when no business rules exist.

## UC chain table

| UC-ID | Title | REQ-IDs | RB-ID | SD-IDs | CLS names | TC-IDs | ADR-IDs | NFR-IDs | BR-NNN |
|---|---|---|---|---|---|---|---|---|---|
| `<PREFIX>-UC-001` | `<title>` | `<PREFIX>-REQ-001` | `<PREFIX>-RB-001` | `<PREFIX>-SD-001` | `<ClassName>` | `<PREFIX>-TC-001` | `<PREFIX>-ADR-001` | `NFR-XXX` | `BR-001, BR-003` |
| `<PREFIX>-UC-002` | `<title>` | `<PREFIX>-REQ-002` | `<PREFIX>-RB-002` | — | `<ClassName>` | — | — | — | — |

**How to populate each column:**

| Column | Source |
|---|---|
| UC-ID | `use-cases/<PREFIX>-UC-XXX-*.md` filename |
| Title | `# <PREFIX>-UC-XXX: <title>` heading |
| REQ-IDs | `## Traceability` → `Upstream:` field |
| RB-ID | `robustness/<PREFIX>-RB-XXX-*.puml` citing this UC |
| SD-IDs | `sequence/<PREFIX>-SD-XXX-*.puml` citing this UC |
| CLS names | Entity nodes on the matching RB |
| TC-IDs | `## Traceability` in `test-cases/<PREFIX>-TC-XXX-*.md` → `UC:` field |
| ADR-IDs | `adrs/<PREFIX>-ADR-XXX-*.md` `## Context` → `Affected use cases:` field |
| NFR-IDs | `nfr-annotations/<PREFIX>-UC-XXX-nfr.md` → `## Applied NFRs` list |
| BR-NNN | `## Business rules cross-reference (Phase 5d)` table in the UC file → `BR-ID` column |

---

## Chain integrity summary

| Check | Pass / Fail | Details |
|---|---|---|
| UCs with no REQ upstream | `<n> PASS / FAIL` | `<list of UC-IDs>` |
| UCs with no RB | `<n> PASS / FAIL` | `<list>` |
| UCs with no TC | `<n> PASS / FAIL` | `<list>` |
| ADRs with no upstream citation (REQ / NFR / UC / BR) | `<n> PASS / FAIL` | `<list of ADR-IDs>` |
| NFRs with no ADR or container-mapping coverage | `<n> PASS / FAIL` | `<list>` |
| BR-NNN cited in ADRs with no match in `business-rules.md` | `<n> PASS / FAIL` | `<list>` (migration mode only) |

---

## Business rules coverage (migration mode only)

> Omit this section when `docs/business-rules.md` is absent.

| BR-ID | Category | Provenance | Linked UCs | Linked ADRs |
|---|---|---|---|---|
| BR-001 | Invariant | EXTRACTED | `<PREFIX>-UC-001, <PREFIX>-UC-002` | `<PREFIX>-ADR-001` |
| BR-002 | Precondition | INFERRED [VERIFY] | `<PREFIX>-UC-001` | — |
| BR-003 | Transition guard | EXTRACTED | `<PREFIX>-UC-001` | `<PREFIX>-ADR-002` |
| (none) | | | | |

**Business rules summary:**
- Total rules: `<N>` (`<N>` EXTRACTED, `<N>` INFERRED [VERIFY], `<N>` AMBIGUOUS)
- Rules with no UC link: `<N>` — `<list BR-IDs>` (investigate — entity names may not match any UC)
- Rules with no ADR link (⚠ Investigate categories — Invariant / Authorization / Transition guard / Workflow / Calculation): `<N>` — `<list BR-IDs>` (flag for Architect)

**How to populate:**
- **Linked UCs** — scan every `use-cases/<PREFIX>-UC-XXX-*.md` for `## Business rules cross-reference`; collect rows where BR-ID matches.
- **Linked ADRs** — scan every `adrs/<PREFIX>-ADR-XXX-*.md` `## Context` for `BR-\d+` patterns; collect ADR-IDs where the BR-ID appears.

---

## Traceability

- **Generated by:** `iconix-traceability` agent
- **Date:** `<YYYY-MM-DD>`
- **Source artifacts read:** `ids.registry.md`, `use-cases/UC-*.md`, `robustness/RB-*.puml`,
  `sequence/SD-*.puml`, `adrs/*.md`, `nfr-annotations/*.md`, `iconix.config.yaml` (NFR catalog),
  `docs/business-rules.md` (when present — migration mode only)
- **Next action:** resolve any FAIL rows above before the next milestone gate
