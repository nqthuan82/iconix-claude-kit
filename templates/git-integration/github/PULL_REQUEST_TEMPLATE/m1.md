## M1 — Requirements PR

## Summary
<one sentence — which feature/UC scope this PR establishes the requirements for>

## Affected use cases
<list UC-IDs, e.g. UC-017 Place Bet, UC-018 Cancel Bet>

## REQs added / changed
<list REQ-IDs with one-line summaries>

## M1 gate checklist
- [ ] Every UC cites ≥1 REQ in `## Traceability`; every REQ is cited by ≥1 UC
- [ ] Every UC has a basic course + ≥1 alternate course, active voice
- [ ] No UC exceeds two paragraphs total
- [ ] Initial domain model exists at `domain-model/domain-model.puml` and was drawn before UC flows
- [ ] Glossary entries exist for every domain noun introduced
- [ ] No "shall" statements inside UC flows (moved to REQ files)
- [ ] No technology-named REQs (frameworks, libs, protocols) — those belong in ADRs
- [ ] Every UC belongs to one UC package overview diagram

Run `/iconix-status` to confirm.

## Reviewer notes
- Domain model in this PR is the **initial** draft (PO scope). Refinement happens at M2 by the Analyst.
- This PR does **not** include robustness diagrams, sequence diagrams, code, or tests.

## Traceability
REQ → UC links:
<paste, e.g. REQ-044 ← UC-017, UC-019>

## Work item
<optional, see iconix.config.yaml git.work_item_prefix>
