## M2 — Preliminary Design PR

## Summary
<one sentence — what design surfaces this PR establishes>

## Affected use cases
<list UC-IDs>

## Artifacts added / changed
- Robustness diagrams: <RB-IDs>
- Refined domain model: <yes/no — what was added>
- Container mapping: <which UCs mapped to which containers>
- ADRs: <ADR-IDs and one-line summaries>
- NFR annotations: <which UCs have NFR-XXX-nfr.md>

## M2 gate checklist
- [ ] Each UC has a robustness diagram (`robustness/RB-XXX-<slug>.puml`)
- [ ] All four robustness rules pass (Boundary↔Actor only; Entity↔Controller only; Controller↔anything; no Boundary↔Boundary; no Entity↔Entity directly)
- [ ] Domain model refined — every entity used in RBs exists with attributes and types
- [ ] Every UC mapped to ≥1 container in `container-mapping/`
- [ ] Every NFR cited by ≥1 ADR
- [ ] No method signatures or types on robustness diagrams (those belong at M3)
- [ ] PDR readiness check passed in `/iconix-status`

## Reviewer notes
- This PR does **not** include sequence diagrams, class methods, code, or tests — those are M3.
- If this PR also touches `arch/`-tier decisions, link or include the relevant `[ARCH]`-tagged commits.

## Traceability
UC → RB → container chain:
<paste, e.g. UC-017 → RB-017 → container "Bet API">

## Work item
<optional>
