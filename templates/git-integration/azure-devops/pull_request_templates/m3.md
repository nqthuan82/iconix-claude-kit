## M3 — Critical Design PR

## Summary
<one sentence — what design + test artifacts this PR delivers>

## Affected use cases
<list UC-IDs>

## Artifacts added / changed
- Sequence diagrams: <SD-IDs>
- Class model entries: <CLS names>
- Test cases: <TC-IDs and one-line summaries>
- Test plan: <yes/no — which UCs added>

## M3 gate checklist
- [ ] Every UC has a sequence diagram (`sequence/SD-XXX-<slug>.puml`)
- [ ] Every controller on the matching RB has corresponding messages on the SD
- [ ] Class model entries have attributes **and** operations
- [ ] Every controller has ≥1 test case
- [ ] Test plan exists at `test-plan/test-plan-<date>.md` and lists every TC
- [ ] No code under `src/` in this PR (Implementation is a separate PR)
- [ ] CDR readiness check passed in `/iconix-status`

## Reviewer notes
- Verify SD message order matches the UC two-column flow.
- Class model attributes must be **typed**.
- Entity classes with ≥2 operations and 0 attributes are flagged "attribute-sparse" by the Reviewer.

## Traceability
SD → CLS → TC chain:
<paste>

## Work item
AB#<N>
